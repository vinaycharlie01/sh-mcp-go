setup:
  modDownload: true
  modTidy: false

build:
  output: dist/sh-mcp-go
  main: ./cmd/sh-mcp-go

test:
  packages:
    - ./...
  args:
    - "-v"
    - "-short"
    - "-timeout=120s"

lint:
  args:
    - "--timeout=10m"
